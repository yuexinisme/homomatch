eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8083/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
server:
  port: 8084
spring:
  application:
    name: client
feign:
  hystrix:
    enabled: true